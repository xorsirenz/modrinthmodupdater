###### minecraft fabric mod auto updater

##### Instructions:
 copy your mods/ directory into this repo, then run main.py, 
 the script will create a backup directory called mods.bak. 
 then move the new mods/ directory to your new mc instance version

```sh
$ pip install -r requirements.txt
$ python3 main.py
``````
